"""Configuration management."""

DEFAULT_PORT = 8080
DEFAULT_HOST = "localhost"

def get_config():
    return {
        "port": DEFAULT_PORT,
        "host": DEFAULT_HOST,
    }
# comment from config branch
# comment from server branch
